<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
 <mapper namespace="test">
      <select id="selectAll" resultType="hello.entity.UserEntity">
          select name,id,color,age from tbl_puppy limit 10
      </select>
      <select id="selectOne" resultType="hello.entity.UserEntity">
          select name,id,color,age from tbl_puppy limit 1
      </select>
  </mapper>
